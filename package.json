{
  "name": "now-enum-parser",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": "ssh://git@project.revoneo.com:2224/active/living-tech/now_enum_parser.git",
  "scripts": {
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
    "start:dev": "tsc --watch",
    "lint": "tslint -p tsconfig.json -c tslint.json"
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "tslint": "^6.1.2",
    "typescript": "^3.9.5"
  }
}
